apiVersion: v1                     # Core API version
kind: Pod                          # Kubernetes Pod resource
metadata:
  name: demo-live-pod              # Pod name
  labels:
    app: demo-live-pod             # App label value
spec:
  containers:
    - name: demo-nginx             # Single container name
      image: nginx:latest          # Demo image tag
      ports:
        - name: http               # Port label
          containerPort: 80        # Default nginx port
          protocol: TCP            # Network protocol
      livenessProbe:               # Container health check
        httpGet:
          path: /                  # Probe path
          port: 80                 # Probe port
        initialDelaySeconds: 10    # Delay before first check
        periodSeconds: 10          # Check frequency
        timeoutSeconds: 1          # Probe timeout
        failureThreshold: 3        # Fail count before restart